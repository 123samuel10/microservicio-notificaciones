# App
DEBUG=false
PORT=8004

# Base de datos
DB_HOST=localhost
DB_PORT=5432
DB_NAME=noti_db
DB_USER=postgres
DB_PASSWORD=postgres

# JWT (mismo secreto compartido)
JWT_SECRET_KEY=cambia-esta-clave-secreta-en-produccion
JWT_ALGORITHM=HS256

# Clave interna entre microservicios (usar un valor largo y aleatorio en producción)
INTERNAL_API_KEY=clave-interna-entre-servicios

# Email (AWS SES / SMTP)
EMAIL_HABILITADO=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=noreply@empleahumboldt.edu.co

# Push (Firebase Cloud Messaging)
PUSH_HABILITADO=false
FCM_SERVER_KEY=
